"""
Command layer for the Code Editor.

Pure-logic modules that the UI delegates to. Anything that touches Maya API,
the filesystem, or heavy text processing lives here so that the UI layer can
stay focused on Qt widgets and user interaction.
"""

# FakeTools Custom Site-Packages Path
# Uncomment and set the path to use a custom install location.
# Packages are stored as: <path>/<maya_version>/site-packages/
# FAKETOOLS_SITE_PACKAGES=D:/my_packages
